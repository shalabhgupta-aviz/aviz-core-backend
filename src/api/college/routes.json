{
  "routes": [
    {
      "method": "GET",
      "path": "/colleges",
      "handler": "college.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/colleges/:id",
      "handler": "college.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}